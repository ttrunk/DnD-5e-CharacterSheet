import os.path

class DnDConfig:
    def __init__(self):
        pass

    def character_traits_file(string):
        userDir = os.path.abspath(os.pardir)
        characterTraits = os.path.join(userDir, 'DnD-5e-CharacterSheet/character_traits.txt')
        return characterTraits

    def ability_score_file(string):
        userDir = os.path.abspath(os.pardir)
        abilityScore = os.path.join(userDir, 'DnD-5e-CharacterSheet/ability_score.txt')
        return abilityScore

    def saving_throw_scores_file(string):
        userDir = os.path.abspath(os.pardir)
        savingThrowScores = os.path.join(userDir, 'DnD-5e-CharacterSheet/saving_throw_scores.txt')
        return savingThrowScores

    def skill_score_file(string):
        userDir = os.path.abspath(os.pardir)
        skillScore = os.path.join(userDir, 'DnD-5e-CharacterSheet/skill_score.txt')
        return skillScore

    def battle_stats_file(string):
        userDir = os.path.abspath(os.pardir)
        battleStats = os.path.join(userDir, 'DnD-5e-CharacterSheet/battle_stats.txt')
        return battleStats

    def attack_stats_file(string):
        userDir = os.path.abspath(os.pardir)
        attackStats = os.path.join(userDir, 'DnD-5e-CharacterSheet/attack_stats.txt')
        return attackStats

    def attack_notes_file(string):
        userDir = os.path.abspath(os.pardir)
        attackNotes = os.path.join(userDir, 'DnD-5e-CharacterSheet/attack_notes.txt')
        return attackNotes

    def per_traits_file(string):
        userDir = os.path.abspath(os.pardir)
        perTraits = os.path.join(userDir, 'DnD-5e-CharacterSheet/personality_traits.txt')
        return perTraits

    def bonds_file(string):
        userDir = os.path.abspath(os.pardir)
        bonds = os.path.join(userDir, 'DnD-5e-CharacterSheet/bonds.txt')
        return bonds

    def ideals_file(string):
        userDir = os.path.abspath(os.pardir)
        ideals = os.path.join(userDir, 'DnD-5e-CharacterSheet/ideals.txt')
        return ideals

    def flaws_file(string):
        userDir = os.path.abspath(os.pardir)
        flaws = os.path.join(userDir, 'DnD-5e-CharacterSheet/flaws.txt')
        return flaws

    def prof_lang_file(string):
        userDir = os.path.abspath(os.pardir)
        prof_lang = os.path.join(userDir, 'DnD-5e-CharacterSheet/prof_lang.txt')
        return prof_lang

    def all_org_file(string):
        userDir = os.path.abspath(os.pardir)
        all_org = os.path.join(userDir, 'DnD-5e-CharacterSheet/all_org.txt')
        return all_org

    def feat_trait_file(string):
        userDir = os.path.abspath(os.pardir)
        feat_trait = os.path.join(userDir, 'DnD-5e-CharacterSheet/feat_trait.txt')
        return feat_trait

    def equip_file(string):
        userDir = os.path.abspath(os.pardir)
        equip = os.path.join(userDir, 'DnD-5e-CharacterSheet/equip.txt')
        return equip

    def treasure_file(string):
        userDir = os.path.abspath(os.pardir)
        treasure = os.path.join(userDir, 'DnD-5e-CharacterSheet/treasure.txt')
        return treasure

    def misc_file(string):
        userDir = os.path.abspath(os.pardir)
        misc = os.path.join(userDir, 'DnD-5e-CharacterSheet/misc.txt')
        return misc

    def other_file(string):
        userDir = os.path.abspath(os.pardir)
        other = os.path.join(userDir, 'DnD-5e-CharacterSheet/other.txt')
        return other




